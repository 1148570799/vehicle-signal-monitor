spring:
  application:
    name: cockpit-diag-agent
  jackson:
    serialization:
      indent-output: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
